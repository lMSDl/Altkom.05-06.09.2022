@page
@model WebAppRazor.Pages.Login.IndexModel
@{
}


<div class="row">
    <div class="col-md-4">

        @using (Html.BeginForm())
        {

            <div class="form-group">
                @Html.Label("Username")
                <br>
                @Html.Editor("Username")
            </div>
            <div class="form-group">
                @Html.Label("Password")
                <br>
                @Html.Password("Password")
            </div>

            <div>
                <input type="submit" value="Login" class="btn" />
            </div>
        }

    </div>
</div>